# This file will be copied to ~/.emscripten if that file doesn't exist. Or, this is that copy.
# IMPORTANT: Edit the *copy* with the right paths!

EMSCRIPTEN_ROOT = os.path.expanduser('{{ path }}/SDK')
LLVM_ROOT = os.path.expanduser('{{ path }}/Library/emscripten-fastcomp/build/bin')
NODE_JS = 'node'
TEMP_DIR = '/tmp'

COMPILER_ENGINE = NODE_JS

JS_ENGINES = [NODE_JS]
